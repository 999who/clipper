# Зависимости описаны в pyproject.toml. Эта строка ставит clipper в режиме
# разработки вместе со всеми зависимостями и командой `clipper`:
#   pip install -r requirements.txt
# Запускать из корня репозитория.
-e .[dev]
