"""Ядро clipper: вся обработка, без интерфейса.

Модули этого пакета не импортируют typer/rich/textual и ничего не печатают.
О ходе работы они сообщают событиями (см. `events.py`), об ошибках — исключениями
`ClipperError` с подсказкой для пользователя (см. `errors.py`). Тяжёлые библиотеки
(faster-whisper, mediapipe, OpenCV) импортируются только внутри функций, которым
они нужны.
"""
